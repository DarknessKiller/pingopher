{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Pingopher",
            "type": "go",
            "request": "launch",
            "program": "${workspaceFolder}/cmd/app/main.go",
            "cwd": "${workspaceFolder}",
            "mode": "debug",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}